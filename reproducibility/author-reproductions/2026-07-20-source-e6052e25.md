# Author-Operated Reproduction — July 20, 2026

## Status

**Reproduction result: PASS**

This record documents an author-operated reproduction of the historical
controlled synthetic experiment.

It is not independent replication, production validation, customer evidence,
or evidence of industry adoption.

## Historical Source Version

Research repository:

`BaharathBathula/self-healing-pipeline-research-paper`

Historical experiment source commit:

`e6052e25b4579ef6698b73c1d7c64b50d547e212`

Commit subject:

`Add full reproducible experiment execution script`

## Clean Environment

The historical source commit was checked out into a separate detached-HEAD
workspace.

Environment:

- Windows 11, 64-bit
- Python 3.12.2
- pandas 3.0.3
- pytest 9.1.0
- dependency versions installed from `requirements-lock.txt`
- dependency-lock SHA-256:
  `6C7C3B4E5C644EEF0DFE7FB42333D52AC97E706BC83E3065D00E0B4C2F30670A`

The clean environment contained the exact 22 package versions recorded in the
dependency lock, and `pip check` reported no broken dependencies.

## Automated Test Result

The exact historical source commit produced:

- 241 tests collected
- 241 tests passed
- 0 test failures

## Experiment Configuration

The reproduced experiment used:

- synthetic row count: 10,000
- repetitions per scenario: 30
- initial random seed: 42
- artifact size: 4,096 bytes
- dataframe trials: 660
- artifact trials: 120
- combined trials: 780

The metadata generated by the reproduction recorded the expected historical
Git commit and configuration.

## Invocation Note

Direct execution of `scripts/run_experiments.py` initially failed before any
trial ran because the project root was not present on Python's import path.

The failed attempt was preserved.

The experiment was then executed successfully with the repository root
explicitly assigned to `PYTHONPATH`. No source file was modified to obtain the
successful run.

## Deterministic Result Comparison

The preserved reference result and reproduced result contained:

- the same 26-column schema;
- the same 780 rows;
- the same 780 unique trial identifiers;
- no missing trials;
- no unexpected trials; and
- identical values in all 24 deterministic fields compared.

Total deterministic cell mismatches:

**0**

The comparison excluded only:

`runtime_milliseconds`

Runtime is environment-dependent and was not expected to match exactly.

## File Hashes

Preserved reference combined CSV:

`ACFD05D8F3CD0DA5A63274ABBA94C58101454A8DE891E52E127124C34D2AFB4A`

Author-reproduced combined CSV:

`BB2F0597CA148CD124FC5E7C36685035BD82CD3EF62A36081CE6270F6BBE502A`

The complete CSV hashes differ because runtime measurements differ. The
deterministic result comparison found no substantive result mismatch.

## Outcome Consistency

The preserved and reproduced outcome distributions matched exactly:

- 690 trials recorded a detected failure;
- 90 trials recorded no detected failure;
- 180 trials awaited human approval;
- 150 trials scheduled recovery;
- 360 trials reached verified recovery;
- 90 trials remained healthy; and
- 120 artifact trials restored their checksum.

## Boundary-Control Interpretation

Thirty low-severity freshness trials use the injected scenario label
`freshness_violation`, but their injected magnitude remains below the detector
threshold.

The raw result fields therefore mark `detection_correct` and
`classification_correct` as false for those 30 rows when compared mechanically
with the injected label.

For evaluation, those rows are treated as boundary controls rather than actual
failures.

Accordingly, the controlled synthetic interpretation remains:

- 690 of 690 actual failures detected;
- 690 of 690 actual failures classified correctly;
- 0 false positives among 90 healthy and boundary controls; and
- 360 of 690 actual failures reaching verified recovery.

## Evidence Boundary

This reproduction supports:

- source-code executability;
- dependency reproducibility;
- automated-test reproducibility;
- trial-population reproducibility; and
- deterministic-result reproducibility.

It does not establish:

- independent replication;
- external adoption;
- production effectiveness;
- commercial impact;
- broad industry significance; or
- equivalent behavior in other environments.
